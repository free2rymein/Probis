[project]
name = "probis-workers"
version = "0.1.0"
description = "Worker foundation for Probis ingestion, replay, scoring, and alert jobs."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pyright]
pythonVersion = "3.11"
typeCheckingMode = "strict"
